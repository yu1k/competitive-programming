#include <iostream>

using namespace std;

int main(){
    char = c;
    cin >> c;
    if(c == ('a' || 'e' || 'i' || 'o' || 'u'){
        cout << "vowel" << endl;
    }
    else{
        cout << "consonant" << endl;
    }
}